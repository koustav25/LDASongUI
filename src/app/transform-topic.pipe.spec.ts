import { TransformTopicPipe } from './transform-topic.pipe';

describe('TransformTopicPipe', () => {
  it('create an instance', () => {
    const pipe = new TransformTopicPipe();
    expect(pipe).toBeTruthy();
  });
});
